source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in bootstrap_styleguide.gemspec.
gemspec

group :development do
  gem 'puma', '5.5.2'
  gem 'sqlite3', '1.4.2'

  gem 'sass-rails'
  gem 'bootstrap'
end

# To use a debugger
# gem 'byebug', group: [:development, :test]
